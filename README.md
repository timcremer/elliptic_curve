# Elliptic Curve (Rust)

Minimal scaffold for implementing elliptic-curve cryptography in Rust.

Build and test:

```bash
cargo build
cargo test
```

Next step: implement finite-field arithmetic in `src/field.rs`.
